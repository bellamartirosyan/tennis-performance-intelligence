# src/forecast.py
import pandas as pd

def forecast_matches(top_players):
    """
    Dummy forecast function to test imports.
    Returns a simple DataFrame from top_players list.
    """
    return pd.DataFrame(top_players)
